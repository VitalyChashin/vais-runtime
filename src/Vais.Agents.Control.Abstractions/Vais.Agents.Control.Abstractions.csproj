<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <RootNamespace>Vais.Agents.Control</RootNamespace>
    <AssemblyName>Vais.Agents.Control.Abstractions</AssemblyName>
    <Description>Control-plane contracts for Vais.Agents — IAgentPolicyEngine, IAuditLog, and related primitives shared across Orleans / in-process / HTTP lifecycle-manager implementations.</Description>
    <PackageTags>agents;ai;llm;control-plane;abstractions;policy;audit</PackageTags>
  </PropertyGroup>

  <ItemGroup>
    <ProjectReference Include="..\Vais.Agents.Abstractions\Vais.Agents.Abstractions.csproj" />
  </ItemGroup>

  <ItemGroup>
    <PackageReference Include="Microsoft.CodeAnalysis.PublicApiAnalyzers">
      <PrivateAssets>all</PrivateAssets>
      <IncludeAssets>runtime; build; native; contentfiles; analyzers; buildtransitive</IncludeAssets>
    </PackageReference>
  </ItemGroup>

  <ItemGroup>
    <AdditionalFiles Include="PublicAPI.Shipped.txt" />
    <AdditionalFiles Include="PublicAPI.Unshipped.txt" />
  </ItemGroup>

</Project>
