apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "vais-agents-runtime.fullname" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "vais-agents-runtime.labels" . | nindent 4 }}
spec:
  replicas: {{ .Values.replicaCount }}
  selector:
    matchLabels:
      {{- include "vais-agents-runtime.selectorLabels" . | nindent 6 }}
  template:
    metadata:
      labels:
        {{- include "vais-agents-runtime.selectorLabels" . | nindent 8 }}
      {{- with .Values.podAnnotations }}
      annotations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
    spec:
      serviceAccountName: {{ include "vais-agents-runtime.serviceAccountName" . }}
      {{- with .Values.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      securityContext:
        {{- toYaml .Values.podSecurityContext | nindent 8 }}
      containers:
        - name: runtime
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          securityContext:
            {{- toYaml .Values.containerSecurityContext | nindent 12 }}
          ports:
            - name: http
              containerPort: 8080
              protocol: TCP
          env:
            - name: ASPNETCORE_URLS
              value: "http://0.0.0.0:8080"
            - name: VAIS_HOSTING_MODE
              value: {{ .Values.hosting.mode | quote }}
            {{- if eq .Values.hosting.mode "clustered" }}
            - name: VAIS_CLUSTERING_BACKEND
              value: {{ .Values.clustering.backend | quote }}
            {{- if .Values.clustering.existingSecret }}
            - name: {{ include "vais-agents-runtime.clusteringEnvName" . }}
              valueFrom:
                secretKeyRef:
                  name: {{ .Values.clustering.existingSecret | quote }}
                  key: {{ .Values.clustering.existingSecretKey | quote }}
            {{- else }}
            - name: {{ include "vais-agents-runtime.clusteringEnvName" . }}
              value: {{ required "clustering.connectionString is required when hosting.mode=clustered and clustering.existingSecret is empty" .Values.clustering.connectionString | quote }}
            {{- end }}
            {{- end }}
            {{- if .Values.opa.enabled }}
            - name: VAIS_OPA_BASEURL
              value: {{ include "vais-agents-runtime.opaBaseUrl" . | quote }}
            - name: VAIS_OPA_FAILMODE
              value: {{ .Values.opa.failMode | quote }}
            - name: VAIS_OPA_DATAPATH
              value: {{ .Values.opa.dataPath | quote }}
            {{- end }}
            {{- if .Values.observability.otel.endpoint }}
            - name: OTEL_EXPORTER_OTLP_ENDPOINT
              value: {{ .Values.observability.otel.endpoint | quote }}
            {{- end }}
            {{- if .Values.observability.otel.console }}
            - name: VAIS_OTEL_CONSOLE
              value: "true"
            {{- end }}
            {{- if .Values.observability.langfuse.project }}
            - name: VAIS_LANGFUSE_PROJECT
              value: {{ .Values.observability.langfuse.project | quote }}
            {{- end }}
            {{- if .Values.auth.jwtAuthority }}
            - name: VAIS_JWT_AUTHORITY
              value: {{ .Values.auth.jwtAuthority | quote }}
            {{- if .Values.auth.jwtAudience }}
            - name: VAIS_JWT_AUDIENCE
              value: {{ .Values.auth.jwtAudience | quote }}
            {{- end }}
            {{- if .Values.auth.serviceAccountPrincipalMapper }}
            - name: VAIS_SA_PRINCIPAL_MAPPER
              value: "true"
            {{- end }}
            {{- end }}
            {{- if .Values.plugins.pythonReloadPolicy }}
            - name: VAIS_PYTHON_PLUGINS_RELOAD_POLICY
              value: {{ .Values.plugins.pythonReloadPolicy | quote }}
            {{- end }}
          livenessProbe:
            httpGet:
              path: /healthz
              port: http
            initialDelaySeconds: {{ .Values.livenessProbe.initialDelaySeconds }}
            periodSeconds: {{ .Values.livenessProbe.periodSeconds }}
            timeoutSeconds: {{ .Values.livenessProbe.timeoutSeconds }}
            failureThreshold: {{ .Values.livenessProbe.failureThreshold }}
          readinessProbe:
            httpGet:
              path: /readyz
              port: http
            initialDelaySeconds: {{ .Values.readinessProbe.initialDelaySeconds }}
            periodSeconds: {{ .Values.readinessProbe.periodSeconds }}
            timeoutSeconds: {{ .Values.readinessProbe.timeoutSeconds }}
            failureThreshold: {{ .Values.readinessProbe.failureThreshold }}
          resources:
            {{- toYaml .Values.resources | nindent 12 }}
          volumeMounts:
            - name: tmp
              mountPath: /tmp
            {{- if .Values.plugins.enabled }}
            - name: plugins
              mountPath: /var/lib/vais/plugins
            {{- end }}
        {{- if eq (include "vais-agents-runtime.opaSidecar" .) "true" }}
        - name: opa
          image: {{ .Values.opa.image | quote }}
          imagePullPolicy: {{ .Values.opa.imagePullPolicy }}
          args:
            - "run"
            - "--server"
            - "--addr=:8181"
            - "--log-format=json"
            {{- if eq (include "vais-agents-runtime.opaBundleMode" .) "true" }}
            - "--config-file=/opa-config/config.yaml"
            {{- else }}
            - "/policies"
            {{- end }}
          {{- if eq (include "vais-agents-runtime.opaBundleMode" .) "true" }}
          {{- $hasSigningEnv := and .Values.opa.bundle.signing.enabled .Values.opa.bundle.signing.existingSecret }}
          {{- $hasAuthEnv := .Values.opa.bundle.serviceAuthTokenSecret }}
          {{- if or $hasSigningEnv $hasAuthEnv }}
          env:
            {{- if $hasSigningEnv }}
            - name: OPA_BUNDLE_SIGNING_KEY
              valueFrom:
                secretKeyRef:
                  name: {{ .Values.opa.bundle.signing.existingSecret | quote }}
                  key: {{ .Values.opa.bundle.signing.existingSecretKey | quote }}
            {{- end }}
            {{- if $hasAuthEnv }}
            - name: BUNDLE_SERVER_TOKEN
              valueFrom:
                secretKeyRef:
                  name: {{ .Values.opa.bundle.serviceAuthTokenSecret | quote }}
                  key: {{ .Values.opa.bundle.serviceAuthTokenSecretKey | quote }}
            {{- end }}
          {{- end }}
          {{- end }}
          ports:
            - name: opa
              containerPort: 8181
              protocol: TCP
          volumeMounts:
            {{- if eq (include "vais-agents-runtime.opaBundleMode" .) "true" }}
            - name: opa-config
              mountPath: /opa-config
              readOnly: true
            - name: opa-tmp
              mountPath: /tmp
            {{- else }}
            - name: opa-policy
              mountPath: /policies
              readOnly: true
            {{- end }}
          livenessProbe:
            httpGet:
              path: /health
              port: opa
            initialDelaySeconds: 5
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /health
              port: opa
            initialDelaySeconds: 2
            periodSeconds: 5
          resources:
            requests:
              cpu: 20m
              memory: 32Mi
            limits:
              memory: 128Mi
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            runAsNonRoot: true
            capabilities:
              drop: ["ALL"]
        {{- end }}
      volumes:
        - name: tmp
          emptyDir: {}
        {{- if .Values.plugins.enabled }}
        - name: plugins
          {{- if .Values.plugins.persistentVolumeClaimName }}
          persistentVolumeClaim:
            claimName: {{ .Values.plugins.persistentVolumeClaimName }}
          {{- else }}
          emptyDir: {}
          {{- end }}
        {{- end }}
        {{- if eq (include "vais-agents-runtime.opaSidecar" .) "true" }}
        {{- if eq (include "vais-agents-runtime.opaBundleMode" .) "true" }}
        - name: opa-config
          configMap:
            name: {{ include "vais-agents-runtime.opaConfigMapName" . }}
        - name: opa-tmp
          emptyDir: {}
        {{- else }}
        - name: opa-policy
          configMap:
            name: {{ include "vais-agents-runtime.opaPolicyConfigMapName" . }}
        {{- end }}
        {{- end }}
      {{- with .Values.nodeSelector }}
      nodeSelector:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.tolerations }}
      tolerations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.affinity }}
      affinity:
        {{- toYaml . | nindent 8 }}
      {{- end }}
