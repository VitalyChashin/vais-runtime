bin/
obj/
out/
*.user
*.suo
.vs/
.idea/
.vscode/
TestResults/
coverage/
*.trx
*.coverage
*.coveragexml
artifacts/
.nuget/
project.lock.json
packages/


# OS
Thumbs.db
.DS_Store
*.stackdump

# docker-compose bind-mount data (Redis dump, Langfuse Postgres, etc.)
deploy/compose/data/

# Local environment file with real API keys (template committed as .env.example)
deploy/compose/.env

# AI assistant local config (vendor-specific, never committed)
.claude/
.mcp.json

# CodeGraph generated index (tool artifact, never committed)
.codegraph/
.code-review-graph/

# Python build artifacts
__pycache__/
*.pyc
*.pyo
.venv/
*.egg-info/

# Sample-local build / run output
samples/**/publish/
samples/**/logs/

# Workspace-private trees (defence in depth — never committed to the OSS repo)
plans/
research/
modules/

# Local CLI re-pack output (used during dogfood testing of CLI changes)
nupkgs/
